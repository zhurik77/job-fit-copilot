// Общие константы Job Fit Copilot.
// Подключается из background.js (importScripts), sidepanel и options (<script>).
// Всё кладётся в globalThis.JFC, чтобы не плодить глобальные переменные.
(function () {
  // Провайдеры API: endpoint, схема заголовка авторизации и формат ответа модели
  // (нужен background.js, чтобы правильно собрать запрос и разобрать ответ).
  const PROVIDERS = {
    nvidia: {
      label: 'NVIDIA NIM',
      endpoint: 'https://integrate.api.nvidia.com/v1/chat/completions',
      authStyle: 'bearer',
      responseStyle: 'openai'
    },
    openai: {
      label: 'OpenAI',
      endpoint: 'https://api.openai.com/v1/chat/completions',
      authStyle: 'bearer',
      responseStyle: 'openai'
    },
    anthropic: {
      label: 'Anthropic',
      endpoint: 'https://api.anthropic.com/v1/messages',
      authStyle: 'anthropic',
      responseStyle: 'anthropic'
    }
  };

  // Ключ 'model' в chrome.storage.local хранит id из этого объекта.
  // Если API отвечает 404 "model not found" — поменять id соответствующей записи здесь.
  const MODELS = {
    deepseek: { id: 'deepseek-ai/deepseek-v4-flash', label: 'DeepSeek V4 Flash', provider: 'nvidia' },
    glm: { id: 'z-ai/glm-5.2', label: 'GLM-5.2', provider: 'nvidia' },
    openai: { id: 'gpt-4.1', label: 'GPT-4.1 (OpenAI)', provider: 'openai' },
    anthropic: { id: 'claude-sonnet-4-6', label: 'Claude Sonnet 4.6 (Anthropic)', provider: 'anthropic' }
  };
  const DEFAULT_MODEL = 'deepseek';

  // Профиль кандидата по умолчанию (см. 02_FEATURES_AND_PROMPTS.md, раздел 1).
  const DEFAULT_PROFILE = `AI-автоматизатор бизнес-процессов / Business Automation & AI Analyst.
7+ лет опыта на рынке электроники: диагностика и ремонт техники (2018-2022)
-> товарная/e-commerce аналитика, unit-экономика, работа с поставщиками,
3 поездки в Китай (2022-2025) -> AI-автоматизация бизнес-процессов, Python,
REST API, MongoDB, проектирование CRM-логики, промпт-инжиниринг, работа с
LLM (2025-сейчас).
Руководил командой из 12 человек (UniversityID, MVP цифровой платформы).
1 место хакатон "Студенческая весна" 2022, 2 место WorldSkills Russia 2021.
Ключевые навыки: автоматизация бизнес-процессов, AI-инструменты,
промпт-инжиниринг, бизнес-анализ, сбор требований, Python, REST API,
unit-экономика, SQL, MongoDB, Wildberries/Ozon, английский B2.
Целевые роли: AI/automation-специалист, бизнес/IT-аналитик, специалист по
внедрению ИИ, n8n/Zapier/Make automation specialist. НЕ подходит: продажи,
преподавание, чистый менеджмент без аналитики/automation, роли уровня
"стажёр".`;

  // Системный промпт fit-check (редизайн 2026-07-27: ledger-разбор, вердикты-решения;
  // 2026-07-29: HR-персона + требование конкретики вместо общих фраз).
  // Индекс совместимости НЕ присылается моделью — UI считает его как 50 + Σ ledger-дельт.
  const FIT_CHECK_SYSTEM_PROMPT = `Ты — опытный HR-консультант (10+ лет в подборе персонала уровня топ рекрутингового
агентства), помогаешь конкретному кандидату быстро понять, стоит ли откликаться
на вакансию. Пиши как для настоящего профессионального разбора, а не общими
фразами: каждый пункт ledger, reasoning, selling_points и flags должны ссылаться
на конкретные факты из профиля кандидата и текста вакансии (годы опыта, названия
инструментов/технологий, домены, цифры, компании) — никаких абстракций вроде
"хороший кандидат" или "есть релевантный опыт" без указания, какой именно.
Верни ТОЛЬКО валидный JSON без markdown-разметки, без \`\`\`, без преамбулы. Формат:
{
  "title": "краткое название вакансии, макс 40 символов",
  "verdict": "ОТКЛИКАТЬСЯ" | "С ОГОВОРКАМИ" | "ПРОПУСТИТЬ",
  "ledger": ["3-6 объектов {"text": "конкретный фактор совпадения или несовпадения со ссылкой на факт из резюме/вакансии", "delta": число от -30 до 30}"],
  "reasoning": "2-3 предложения на русском, почему такой вердикт — с опорой на конкретные факты, не общими словами",
  "selling_points": ["1-3 конкретных пункта из опыта кандидата, которые стоит подчеркнуть именно в этом отклике"],
  "flags": ["1-3 пункта: чего не хватает, риски, на что обратить внимание"],
  "effort_minutes": "число от 5 до 30: сколько минут займёт вдумчивый отклик с кастомным письмом",
  "booster": "объект {"text": "один конкретный совет, что подняло бы совпадение", "delta": число от 1 до 15} или null, если советовать нечего"
}
Правила ledger: базовый уровень совпадения — 50. Каждый пункт сдвигает его на delta
(положительную или отрицательную). Итог (50 + сумма дельт) должен лежать в пределах
0..100 и честно отражать совпадение. Вердикты: ОТКЛИКАТЬСЯ — сильное совпадение,
С ОГОВОРКАМИ — пограничное, ПРОПУСТИТЬ — нерелевантно. Будь придирчивым — задача
не подбодрить кандидата, а сэкономить его время, отсеяв нерелевантные вакансии.`;

  // Системный промпт сопроводительного письма (см. 02_FEATURES_AND_PROMPTS.md, раздел 3;
  // 2026-07-29: письмо должно быть готово к отправке без правок — без плейсхолдеров).
  // lang: 'ru' — русский; 'en' — английский (LinkedIn/Upwork).
  function letterPrompt(lang) {
    const base = `Напиши готовое к отправке без каких-либо правок сопроводительное письмо на русском
языке от лица кандидата работодателю. Требования: 120-180 слов, без канцелярита
и штампов ("хотел бы выразить заинтересованность"), пиши от первого лица живым
деловым языком, обязательно упомяни 1-2 конкретных факта из опыта кандидата
(с цифрами/названиями, а не общими словами), которые релевантны именно этой
вакансии, заверши конкретным предложением созвониться или обсудить детали.
ЗАПРЕЩЕНО использовать плейсхолдеры и квадратные/угловые скобки вида
"[Название компании]", "[Ваше имя]", "<company>" — используй только реальные
факты, которые есть в переданных данных о вакансии и профиле кандидата; если
какого-то факта (например, имени рекрутера) не хватает, просто не упоминай
его вообще, а не вставляй заглушку. Верни только текст письма, без заголовков
и пояснений — то, что можно скопировать и отправить как есть.`;
    if (lang === 'en') {
      return base.replace(
        'на русском языке',
        'на английском языке, в деловом, но не сухом тоне, характерном для американского/европейского рынка,'
      );
    }
    return base;
  }

  // Промпт оценки самого резюме (без привязки к вакансии) — та же ledger-схема,
  // что и в fit-check, чтобы переиспользовать computeScore()/renderLedger() в UI.
  const RESUME_REVIEW_SYSTEM_PROMPT = `Ты — опытный HR-консультант (10+ лет в подборе персонала), оцениваешь силу и
качество самого резюме кандидата — независимо от конкретной вакансии, её тут
нет. Оцени, насколько резюме убедительно продаёт кандидата: легко ли за 30
секунд понять его ценность, есть ли конкретика (цифры, инструменты, результаты)
или всё расплывчато. Верни ТОЛЬКО валидный JSON без markdown-разметки, без
\`\`\`, без преамбулы. Формат:
{
  "ledger": ["3-6 объектов {"text": "конкретный фактор — что усиливает или ослабляет резюме, со ссылкой на факт из текста", "delta": число от -20 до 20}"],
  "reasoning": "2-3 предложения на русском — общий вывод, с опорой на конкретику из резюме",
  "strengths": ["1-3 конкретных сильных места резюме"],
  "gaps": ["1-3 конкретных слабых места: расплывчатые формулировки, нет цифр/результатов, нет структуры, устаревшая информация и т.п."],
  "action_items": ["2-4 конкретных, выполнимых правки текста резюме — что именно переписать"]
}
Правила ledger: базовый уровень — 50 (среднее резюме). Каждый пункт сдвигает
его на delta. Итог (50 + сумма дельт) должен лежать в пределах 0..100. Будь
придирчивым и конкретным — цель не похвалить, а реально помочь сделать резюме
сильнее.`;

  function genId() {
    return 'p_' + Date.now().toString(36) + Math.random().toString(36).slice(2, 7);
  }

  // Именованные профили кандидата: profiles: [{id, name, text, source, sourceUrl?, updatedAt}],
  // activeProfileId — какой из них используется по умолчанию для проверки/письма.
  // Мигрирует старую схему (единственный profile:string) один раз при первом обращении.
  async function ensureProfiles() {
    const { profiles, activeProfileId, profile } = await chrome.storage.local.get(['profiles', 'activeProfileId', 'profile']);
    if (Array.isArray(profiles) && profiles.length) {
      const validActiveId = (activeProfileId && profiles.some(p => p.id === activeProfileId))
        ? activeProfileId
        : profiles[0].id;
      return { profiles, activeProfileId: validActiveId };
    }
    const seeded = [{
      id: genId(),
      name: 'Профиль',
      text: (profile && profile.trim()) || DEFAULT_PROFILE,
      source: 'manual',
      updatedAt: Date.now()
    }];
    await chrome.storage.local.set({ profiles: seeded, activeProfileId: seeded[0].id });
    return { profiles: seeded, activeProfileId: seeded[0].id };
  }

  globalThis.JFC = { PROVIDERS, MODELS, DEFAULT_MODEL, DEFAULT_PROFILE, FIT_CHECK_SYSTEM_PROMPT, RESUME_REVIEW_SYSTEM_PROMPT, letterPrompt, genId, ensureProfiles };
})();
